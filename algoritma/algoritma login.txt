Tampilkan tampilan Login
input username dan password

if (pilih = login)
	check username dan password
	if (check = "true")
		check status username
		if(check = "siswa")
			tampilkan menu utama siswa
		else
			tampilkan menu utama admin
		end if
	else
		tampilkan pesan "username atau password salah"
		kembali ke tampilan login
	end if
	
end if
