tampilkan tampilan ubah data kelas
tampilkan tampilan button ubah nama kelas
tampilkan tampilan button ubah wali kelas
tampilkan tampilan button ubah ketua kelas
tampilkan tampilan button tambah siswa
tampilkan tampilan button hapus siswa


if (pilih = ubah nama kelas)
	tampilkan tampilan ubah nama kelas
	input nama kelas
	if (balasan pesan api  = OK4)
		tampilkan pesan "nama kelas berhasil diubah"
		kembali ke menu master kelas
	else
		tampilkan pesan dari API
		kembali ke tampilan ubah data kelas
	end if
end if

if (pilih = ubah wali kelas)
	tampilkan tampilan daftar staf 
	pilih staf
	if (balasan pesan api  = OK4)
		tampilkan pesan "wali kelas berhasil diubah"
		kembali ke tampilan ubah data kelas
	else
		tampilkan pesan dari API
		kembali ke tampilan ubah data kelas
	end if
end if

if (pilih = ubah ketua kelas)
	tampilkan tampilan daftar cari siswa
	pilih siswa

	if (balasan pesan api  = OK4)
		tampilkan pesan "ketua kelas berhasil diubah"
		kembali ke tampilan ubah data kelas
	else
		tampilkan pesan dari API
		kembali ke tampilan ubah data kelas
	end if
end if

if (pilih = tambah siswa)
	tampilkan tampilan daftar cari siswa
	cari siswa
	pilih siswa

	if (balasan pesan api  = OK4)
		tampilkan pesan "siswa berhasil ditambah dalam kelas"
		kembali ke tampilan daftar cari siswa
	else
		tampilkan pesan dari API
		kembali ke tampilan daftar cari siswa
	end if
end if

if (pilih = hapus siswa)
	tampilkan tampilan daftar siswa
	pilih siswa
	if (balasan pesan api  = OK4)
		tampilkan pesan "siswa berhasil dihapus dalam kelas"
		kembali ke tampilan daftar siswa
	else
		tampilkan pesan dari API
		kembali ke tampilan daftar siswa
	end if
end if
