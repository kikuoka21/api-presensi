tampilkan tampilan menu master kelas
tampilkan tampilan button input kelas
tampilkan tampilan button lihat data kelas
tampilkan tampilan button ubah data kelas
tampilkan tampilan button ganti password kelas
tampilkan tampilan button hapus kelas

if (pilih = input kelas baru)
	tampilkan tampilan input kelas
	input data kelas
	if (balasan pesan api  = OK4)
		tampilkan pesan "input kelas berhasil"
		kembali ke menu master kelas
	else
		tampilkan pesan dari API
		kembali ke tampilan input kelas
	end if
end if

if (pilih = lihat data kelas)
	tampilkan tampilan cari kelas
	input tahun ajar
	if (balasan pesan api  = OK4)
		tampilkan daftar kelas
		pilih kelas
		tampilkan data kelas
	else
		tampilkan pesan dari API
		kembali ke menu master kelas
	end if
end if

if (pilih = ubah data kelas)
	tampilkan tampilan cari kelas
	input tahun ajar
	if (balasan pesan api  = OK4)
		tampilkan daftar kelas
		pilih kelas
		tampilkan tampilan menu ubah data kelas
	else
		tampilkan pesan dari API
		kembali ke menu master kelas
	end if
end if

if (pilih = hapus kelas)
	tampilkan tampilan cari kelas
	input tahun ajar
	if (balasan pesan api  = OK4)
		tampilkan daftar kelas
		pilih kelas
        input password kelas
        if (balasan pesan api  = OK4)
            tampilkan pesan "hapus kelas berhasil"
            kembali ke menu master kelas
        else
            tampilkan pesan dari API
            kembali ke tampilan daftar kelas
        end if
	else
		tampilkan pesan dari API
		kembali ke menu master kelas
	end if
end if