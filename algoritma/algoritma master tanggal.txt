tampilkan tampilan menu master tanggal
tampilkan tampilan button input tanggal
tampilkan tampilan button hapus tanggal

if (pilih = input tanggal)
	tampilkan tampilan input tanggal
	input tanggal dan keterangan
	if (balasan pesan api  = OK4)
		tampilkan pesan "input tanggal berhasil"
		kembali ke tampilan input tanggal
	else
		tampilkan pesan dari API
		kembali ke menu master tanggal
	end if
end if

if (pilih = hapus tanggal)
	tampilkan tampilan daftar tanggal
	pilih tanggal dan input password
	if (balasan pesan api  = OK4)
		tampilkan pesan "hapus tanggal berhasil"
		kembali ke tampilan daftar tanggal
	else
		tampilkan pesan dari API
		kembali ke menu master tanggal
	end if
end if