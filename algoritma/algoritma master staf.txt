tampilkan tampilan menu master staf
tampilkan tampilan button input staf
tampilkan tampilan button lihat data staf
tampilkan tampilan button ubah data staf
tampilkan tampilan button ganti password staf
tampilkan tampilan button hapus staf

if (pilih = input staf baru)
	tampilkan tampilan input staf
	input data staf
	if (balasan pesan api  = OK4)
		tampilkan pesan "input staf berhasil"
		kembali ke menu master staf
	else
		tampilkan pesan dari API
		kembali ke tampilan tampilan input staf
	end if
end if

if (pilih = lihat data staf)
	tampilkan tampilan cari staf
	input nama atau nip
	if (balasan pesan api  = OK4)
		tampilkan daftar staf
		pilih staf
        tampilkan data staf
	else
		tampilkan pesan dari API
		kembali ke menu master staf
	end if
end if

if (pilih = ubah data staf)
	tampilkan tampilan cari staf
	input nama atau nip
	if (balasan pesan api  = OK4)
		tampilkan daftar staf
		pilih staf
        tampilkan data staf
        ubah data staf
        if (balasan pesan api  = OK4)
            tampilkan pesan "ubah staf berhasil"
            kembali ke tampilan menu master staf
        else
            tampilkan pesan dari API
            kembali ke tampilan daftar staf
        end if
	else
		tampilkan pesan dari API
		kembali ke menu master staf
	end if
end if

if (pilih = ubah password staf)
	tampilkan tampilan cari staf
	input nama atau nip
	if (balasan pesan api  = OK4)
		tampilkan daftar staf
		pilih staf)
        tampilkan tampilan ubah password
        input password dan ulangi password
        if (balasan pesan api  = OK4)
            tampilkan pesan "password staf berhasil diubah"
            kembali ke tampilan menu master staf
        else
            tampilkan pesan dari API
            kembali ke tampilan daftar staf
        end if
	else
		tampilkan pesan dari API
		kembali ke menu master staf
	end if
end if

if (pilih = hapus staf)
	tampilkan tampilan cari staf
	input nama atau nip
	if (balasan pesan api  = OK4)
		tampilkan daftar staf
		if (pilih = staf)
			input password staf
			if (balasan pesan api  = OK4)
				tampilkan pesan "hapus staf berhasil"
				kembali ke tampilan menu master staf
			else
				tampilkan pesan dari API
				kembali ke menu master staf
			end if
		end if
	else
		tampilkan pesan dari API
		kembali ke menu master staf
	end if
end if