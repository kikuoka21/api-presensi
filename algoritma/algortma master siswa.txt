tampilkan tampilan menu master siswa
tampilkan tampilan button input siswa
tampilkan tampilan button lihat data siswa
tampilkan tampilan button ubah data siswa
tampilkan tampilan button ganti password siswa
tampilkan tampilan button hapus siswa

if (pilih = input siswa baru)
	tampilkan tampilan input siswa
	input data siswa
	if (balasan pesan api  = OK4)
		tampilkan pesan "input siswa berhasil"
		kembali ke tampilkan menu master siswa
	else if (balasan pesan api  = "TOKEN1" || balasan pesan api  = "TOKEN2" )
		tampilkan pesan dari API
		tampilkan tampilan login
	else
		tampilkan pesan dari API
		kembali ke menu master siswa
	end if
end if

if (pilih = lihat data siswa)
	tampilkan tampilan cari siswa
	input tahun lahir
	input nama atau nis
	if (balasan pesan api  = OK4)
		tampilkan list siswa
		if (pilih = siswa)
			tampilkan data siswa
		end if
	else if (balasan pesan api  = "TOKEN1" || balasan pesan api  = "TOKEN2" )
		tampilkan pesan dari API
		tampilkan tampilan login
	else
		tampilkan pesan dari API
		kembali ke menu master siswa
	end if
end if

if (pilih = ubah data siswa)
	tampilkan tampilan cari siswa
	pilih tahun lahir 
	input nama atau nis
	if (balasan pesan api  = OK4)
		tampilkan list siswa
		if (pilih = siswa)
			tampilkan data siswa
			ubah data siswa
			if (balasan pesan api  = OK4)
				tampilkan pesan "ubah siswa berhasil"
				kembali ke tampilkan list siswa
			else 
				tampilkan pesan dari API
				kembali ke menu master siswa
			end if
		end if
	else if (balasan pesan api  = "TOKEN1" || balasan pesan api  = "TOKEN2" )
		tampilkan pesan dari API
		tampilkan tampilan login
	else
		tampilkan pesan dari API
		kembali ke menu master siswa
	end if
end if

if (pilih = ubah password siswa)
	tampilkan tampilan cari siswa
	pilih tahun lahir 
	input nama atau nis
	if (balasan pesan api  = OK4)
		tampilkan list siswa
		if (pilih = siswa)
			tampilkan tampilan ubah password
			input password dan ulangi password
			if (balasan pesan api  = OK4)
				tampilkan pesan "password siswa berhasil diubah"
				kembali ke tampilkan list siswa 
			else 
				tampilkan pesan dari API
				kembali ke menu master siswa
			end if
		end if
	else if (balasan pesan api  = "TOKEN1" || balasan pesan api  = "TOKEN2" )
		tampilkan pesan dari API
		tampilkan tampilan login
	else
		tampilkan pesan dari API
		kembali ke menu master siswa
	end if
end if

if (pilih = hapus siswa)
	tampilkan tampilan cari siswa
	pilih tahun lahir 
	input nama atau nis
	if (balasan pesan api  = OK4)
		tampilkan list siswa
		if (pilih = siswa)
			input password staf
			if (balasan pesan api  = OK4)
				tampilkan pesan "hapus siswa berhasil"
				kembali ke tampilkan list siswa 
			else if (balasan pesan api  = "TOKEN1" || balasan pesan api  = "TOKEN2" )
				tampilkan pesan dari API
				tampilkan tampilan login
			else
				tampilkan pesan dari API
				kembali ke menu master siswa
			end if
		end if
	else if (balasan pesan api  = "TOKEN1" || balasan pesan api  = "TOKEN2" )
		tampilkan pesan dari API
		tampilkan tampilan login
	else
		tampilkan pesan dari API
		kembali ke menu master siswa
	end if
end if